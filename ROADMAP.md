# BioZone — خارطة طريق تطوير لوحة الموظفين والمتجر

> **الغرض من الملف ده:** أي حد يكمل الشغل (حتى لو معاهوش سياق المحادثة اللي طلعت بيها الخطة) يقدر يفهم منه: اتعمل إيه، اتقرر إيه، واللي لسه محتاج تنفيذ فين بالظبط.
>
> **آخر تحديث:** لسه في مرحلة التخطيط — **مفيش كود اتكتب من الخطة دي لحد دلوقتي**. كل اللي في الملف ده قرارات وترتيب أولويات بس.

---

## 0. الأساس الموجود فعلاً في المشروع (Baseline)

مهم توثيقه عشان محدش يعيد بناء حاجة موجودة أصلًا:

| الحاجة | فين | ملاحظة |
|---|---|---|
| Sidebar ثابت + Topbar + notification bell | `staff/templates/staff/base.html` | permission-gated بالكامل |
| Breadcrumbs | `{% crumb %}` tag في `staff_ui.py` | مستخدم في صفحات، مش كلها |
| Notebook tabs + عدادات (Smart Header جزئي) | `staff/templates/staff/clients/detail.html` | نمط جاهز نطبّقه على باقي الكيانات |
| Status badges | `{% status_badge %}` tag | مربوط بـ `Order.Status` وحالة العميل |
| Activity timeline | `orders.models.OrderLog` (CREATED / STATUS_CHANGED / NOTE) | **خاص بالطلبات بس حاليًا** |
| htmx boost + view transitions | `base.html` | تنقل سلس بالفعل |
| طباعة / PDF | `staff/templates/staff/orders/print.html` | للطلبات بس |
| استيراد/تصدير Excel للمنتجات | `products/services/import_export/` | يحل جزء كبير من احتياج "quick create" بالجملة |
| Dashboard + تقارير تفصيلية لكل موديول | `staff/dashboard.html` + `staff/templates/staff/reports/*` | يغطي عمليًا كل احتياج "dashboard widgets" |
| حد أدنى لقيمة الطلب | `orders.models.SiteConfig.min_order_amount` | **Singleton عام لكل العملاء — مفيش تخصيص لكل عميل** |
| مقاسات الوحدة (قطعة/كرتونة) | `ProductUnit.size` (S/L) | **ده مش "مقاس منتج" — لا تخلط بينه وبين مقاس المنتج (لبس/هدوم) اللي كان بيتكتب كنص حر جوه `name_ar` — القرار اتحسم، شوف مرحلة 5** |
| منتجات مشابهة/مكمّلة (Cross-sell) | غير موجود خالص | ميزة جديدة بالكامل (مرحلة 5) — **مختلفة عن Product Variants تحت** |
| مقاسات/تنويعات نفس الصنف (Product Variants) | غير موجود خالص | ميزة جديدة بالكامل (مرحلة 5) |

### قرار محسوم: قضية bidi في `Product.name_ar`
كانت المشكلة: اسم المنتج بيتكتب فيه توكن لاتيني (زي مقاس "LX") مدموج مع نص عربي في نفس الحقل، وخوارزمية الـ Unicode Bidi كانت بتشوّه ترتيب العرض في صفحات المتجر.

**القرار:** الحل الكامل، مش الـ patch السريع — إضافة حقل `Product.size` منفصل فعلي (مقاس لبس/هدوم، مختلف تمامًا عن `ProductUnit.size`)، مع migration بتستخرج المقاس من الأسماء الحالية بـ regex، وعرضه كـ badge منفصل في كارت الصنف بالمتجر. تفاصيل التنفيذ الكاملة وربطها بنظام Product Variants موضحة في **مرحلة 5**.

---

## 1. المراحل (بالترتيب)

كل مرحلة مستقلة قدر الإمكان، تقدر تتسلّم وتتجرّب لوحدها، وتقدر تتوزّع على أكتر من مطوّر بالتوازي طالما محدش بيلمس نفس الملفات.

### مرحلة 1 — تعميم الـ Pattern الموجود
**الحالة: لم يبدأ**
- Breadcrumbs + tabs + smart header counters على المنتجات/المخزون/الموردين (نفس نمط `clients/detail.html`).
- مفيش تغيير موديلات. views/templates بس.
- **معيار القبول:** كل صفحة تفاصيل (منتج، صنف مخزون) عندها نفس بنية التابات والعدادات القابلة للضغط زي العميل بالظبط.

### مرحلة 2 — بنية تحتية (Audit + Chatter + Relations)
**الحالة: لم يبدأ — تحتاج migration**
- **Generic Audit Log** (ContentType-based) بدل ما يفضل `OrderLog` خاص بالطلبات بس.
- **Chatter عام:** تعميم نوع `NOTE` الموجود في `OrderLog` ليشتغل على أي كيان (عميل/منتج) — ملاحظات داخلية بين الموظفين تفضل ظاهرة على السجل. (فكرة من Odoo — تكلفة شبه صفرية لو اتعملت مع الـ Audit Log لأن البنية واحدة).
- Related Documents links (منتج → مبيعات/مشتريات/مخزون).
- **معيار القبول:** فتح أي سجل (عميل/منتج) يوريك مين عدّل إيه وإمتى + تقدر تسيب ملاحظة داخلية عليه.

### مرحلة 3 — ترقية الجداول
**الحالة: لم يبدأ**
- Sortable + sticky header + pagination + bulk actions + filter panel + grouping + بحث متقدم.
- **Quick Edit inline** (من WordPress): تعديل حقل بسيط (سعر، is_active) من صف الجدول مباشرة من غير فتح صفحة تعديل كاملة.
- **بدون** resizable columns / column visibility (قيمتها منخفضة لأداة داخلية بأعمدة ثابتة معروفة، تكلفة أعلى من العائد).
- **معيار القبول:** جدول المنتجات يقدر يتفرز/يتصفّح صفحات/يتعمله bulk activate-deactivate، وتعديل السعر يحصل من غير مغادرة الصفحة.

### مرحلة 4 — Lifecycle + Action Bar
**الحالة: لم يبدأ — تبني على مرحلة 2**
- Visual status bar للطلبات (مبني على `Order.Status` الموجود).
- Unified action menu (طباعة/تصدير/أرشفة).
- **تكرار سجل (Duplicate)** من WordPress: نسخ منتج موجود كنقطة بداية بدل ملء فورم من الصفر.
- **علامات (Tags) — موديل واحد عام مش خاص بالطلبات:** بدل ما نبني "Order tags" كموديل مقفول على الطلبات بس، يتصمم موديل `Tag` عام واحد (اسم + لون) + علاقة عامة (Generic FK أو M2M) يشتغل على أي كيان. النهارده بيتستخدم للطلبات ("عاجل"، "يحتاج مراجعة")، وبعدين لو احتجنا تاج على منتج أو عميل، بنستخدم نفس الموديل من غير كود جديد.

### مرحلة 5 — تحسينات واجهة المتجر (Storefront) — جديدة، من مقترحات العميل
**الحالة: ✅ مكتملة — راجع PROGRESS.md لتفاصيل التنفيذ الكاملة**

هذه المرحلة تجمع مقترحات صاحب المنتج + مقترحات المتاجر المشهورة، لأنها كلها بتلمس نفس الصفحات (المتجر/صفحة المنتج):

1. **صفحة منتج تعرض منتجات مشابهة ومكمّلة (قسمين منفصلين، اختيار يدوي في الاتنين):**
   - **الموديل:** حقلين منفصلين على `Product` (مش حقل واحد)، عشان المنطق مختلف ومتفصّل عن بعضه بوضوح:
     ```python
     similar_products = models.ManyToManyField('self', blank=True, symmetrical=False, related_name='similar_of')
     complementary_products = models.ManyToManyField('self', blank=True, symmetrical=False, related_name='complementary_of')
     ```
     مفيش اشتقاق تلقائي من `category` دلوقتي — اختيار يدوي بحت في الاتنين من كارت الصنف في staff، أبسط وأوضح وأسهل صيانة. لو احتجنا اقتراح تلقائي بعدين، بيتضاف فوق الموجود من غير ما يعدّل فيه.
   - **مكان الإدخال (staff):** كارت الصنف (`staff/templates/staff/products/form.html`) يضيف الاتنين، لكن كـ **partial منفصل** مش جوه الفورم نفسه:
     `staff/templates/staff/products/partials/related_products_section.html` — بيتضم بـ `{% include %}`، بالظبط زي كارت "الوحدات والأسعار" الموجود حاليًا كقسم بصري منفصل.
   - **مكوّن الاختيار (Product Picker):** بحث + شرائح (chips) بـ htmx، من غير مكتبات JS خارجية جديدة. يُبنى كـ **component واحد قابل لإعادة الاستخدام** (مش خاص بالعلاقة دي بس) — هيفيد أي علاقة اختيار مستقبلية تانية (Order tags في مرحلة 4، أي ربط سجلات في مرحلة 2).
   - **شكل العرض في المتجر:** قسم عريض منفصل لكل من "مشابهة" و"مكمّلة" (Component واحد بيتعاد استخدامه مرتين بـ parameter يحدد نوع العلاقة، مش تكرار كود)، بيتحرك (carousel) **من اليمين للشمال** (متوافق مع RTL)، مع زرار "عرض المزيد".
2. **إشارة عند التحويم (hover) على كارت المنتج في المتجر — نسخة مبسّطة بدون أي استعلام إضافي:** التلميح بيظهر **على كل الكروت بشكل موحّد** (مش شرطي على وجود مقاسات/منتجات مرتبطة فعليًا)، فمفيش أي حساب أو query إضافي على شبكة المتجر خالص — الأمر كله CSS/HTML بحت (ظل أو أيقونة "تفاصيل أكتر" تظهر عند hover). هيبان صح دايمًا لأن صفحة تفاصيل أي منتج فيها معلومات أكتر من الكارت أصلًا (الوصف، الوحدات) حتى لو الصنف مالوش مقاسات أو منتجات مرتبطة.
3. **إعادة الطلب بضغطة واحدة (Reorder)** (من متاجر مشهورة — Shopify/WooCommerce pattern): زرار على أي طلب سابق يملّي السلة بنفس الأصناف، مفيد جدًا لو العملاء بيكرروا نفس طلبية المستلزمات الطبية بشكل دوري.
4. **مقاسات/تنويعات نفس الصنف (Product Variants — فكرة من Odoo، مختلفة عن بند 1 فوق):**
   - **الفرق عن "منتجات مشابهة/مكمّلة":** دي مش توصية بيع لمنتج تاني، دي **نفس الصنف بالظبط** (نفس الاسم، نفس الصورة غالبًا) بس بمقاس مختلف — فبيتاخد موديل منفصل تمامًا عشان محدش يلخبط بينه وبين `similar_products`/`complementary_products`.
   - **الموديل:** موديل خفيف جديد `ProductVariantGroup` (مجرد id + اسم داخلي اختياري للإدارة)، بدل self-FK غامض ميوضّحش مين الأصل ومين الفرع:
     ```python
     class ProductVariantGroup(models.Model):
         internal_name = models.CharField(max_length=255, blank=True)  # للإدارة بس، مش بيظهر للعميل
     ```
   - **حقلين على `Product`:**
     ```python
     size = models.CharField(max_length=50, blank=True)  # مقاس المنتج (لبس/هدوم) — غير ProductUnit.size تمامًا
     variant_group = models.ForeignKey(ProductVariantGroup, null=True, blank=True, related_name='products', on_delete=models.SET_NULL)
     ```
   - **في كارت الصنف بـ staff:** لو المنتج له `variant_group`، تقدر تربطه بمقاسات تانية من نفس الصنف (كل واحد منهم Product مستقل بمخزونه/وحداته/كوده الخاص — مفيش تغيير في `Inventory` أو `ProductUnit`، الربط بس على مستوى `Product`).
   - **في كارت الصنف بالمتجر:** لو `variant_group` موجود، يظهر صف اختيار مقاسات (chips) جنب اسم المنتج، مبني على باقي أصناف نفس المجموعة. **لو مقاس معيّن رصيده صفر، يفضل ظاهر لكن معطّل بصريًا (مش مخفي)** — العميل يشوف كل المقاسات المتاحة لنفس الصنف من غير ما يحتاج يفتح صفحة منتج جديدة.
   - **معيار القبول:** فتح صنف له مقاسات بديلة يوريك اختيار المقاس في نفس الصفحة، والمقاس الغير متوفر يبان معطّل مش مختفي، واختيار مقاس تاني ينقلك لصفحة الصنف بتاعه (بدون فقدان السياق البصري — نفس الاسم/القسم/الفتات).
- **معيار القبول (للبنود 1-3):** فتح أي منتج في المتجر يوريك قسمين (مشابه/مكمّل) قابلين للتصفح، وكارت المنتج بيوضح بصريًا إن فيه تفاصيل أكتر عند hover.

### مرحلة 6 — الحد الأدنى للطلب لكل عميل — جديدة، من مقترحات العميل
**الحالة: ✅ مكتملة — راجع PROGRESS.md وAPPLY_INSTRUCTIONS_phase6.md لتفاصيل التنفيذ الكاملة**

المشكلة الحالية: `SiteConfig.min_order_amount` قيمة واحدة عامة لكل العملاء (Singleton)، ومفيش حد أدنى مرتبط بالعميل نفسه.

**التصميم المقترح:**
- إضافة حقل `min_order_amount` (nullable) على `ClientProfile` (وليس `AccountType` — العميل طلبها *لكل عميل* تحديدًا مش لكل نوع حساب).
- **منطق الحساب الفعلي:** لو `ClientProfile.min_order_amount` متحدد (مش None) يُستخدم هو، وإلا يُستخدم `SiteConfig.min_order_amount` العام كقيمة افتراضية (fallback) — بكده مفيش كسر لأي عميل حالي، والقيمة العامة تفضل شغالة كخط أساس.
- تعديل `orders/views/cart.py` و`orders/views/checkout.py` (اللي بيستخدموا `config.min_order_amount` حاليًا) ليقرأوا القيمة الفعّالة (effective value) دي بدل القيمة العامة مباشرة.
- واجهة الإدارة: إضافة الحقل في صفحة تعديل بيانات العميل (`staff/templates/staff/clients/detail.html` أو فورم تعديل منفصل).
- **معيار القبول:** تعديل الحد الأدنى لعميل معيّن مايأثرش على باقي العملاء، وعميل من غير قيمة مخصّصة يفضل شغال بالقيمة العامة القديمة زي ما هي.

### مرحلة 7 — أتمتة ومتابعة (Odoo-inspired)
**الحالة: تم ✅ — راجع PROGRESS.md وAPPLY_INSTRUCTIONS_phase7.md**
- **Activity / متابعة مجدولة:** "اتصل بالعميل X يوم كذا"، مربوطة بموظف ومتابعة، بتحل مشكلة تتبّع حالات `PENDING` والمتأخرين في السداد اللي حاليًا بتتم بالذاكرة بس.
- **مقترحات توريد تلقائي:** تجميع كل الأصناف تحت `min_quantity` في صفحة واحدة "مقترحات التوريد" بدل تنبيه متفرق — البيانات جاهزة أصلًا (`min_quantity` موجود)، الشغل كله في الـ view.

---

## 2. قواعد معمارية عامة — تنطبق على كل مرحلة من غير استثناء

القاعدة الأساسية: **كود بسيط وواضح دلوقتي، قابل للتوسع بعدين من غير إعادة كتابة.** يعني نضيف الحد الأدنى اللي المرحلة الحالية محتاجاه فعلًا، بس نصممه بشكل ميضطرش حد يكسره أو يعيد بناءه لما مرحلة تانية توصل.

### أ. تقسيم الملفات (لا لملف واحد ضخم)
- كل قسم/بطاقة في أي صفحة (فورم، تفاصيل، جدول) = **partial منفصل** يتضم بـ `{% include %}`، مش كله في ملف واحد. مثال قائم بالفعل: كارت "الوحدات والأسعار" منفصل بصريًا عن "بيانات المنتج" — نكمل بنفس المنطق (مثال: `partials/related_products_section.html` لمرحلة 5).
- لو ملف تعدّى ~150-200 سطر، ده إشارة إنه محتاج يتقسّم لـ partials، مش إضافة منطق فوق بعضه.

### ب. تسمية واضحة وثابتة
- أسماء الحقول/الموديلات توصف الغرض مباشرة، من غير اختصارات غامضة: `min_order_amount` على `ClientProfile` (مش `min_amt` أو `threshold`)، `similar_products` / `complementary_products` (مش `related_1` / `related_2`).
- أي حقل جديد لازم يتوثّق بتعليق عربي قصير فوقه (زي الأسلوب المتبع فعلاً في `products/models.py`) يوضح الغرض والفرق عن أي حقل مشابه (مثال: التعليق اللي فرّق `ProductUnit.size` عن مقاس المنتج نفسه).

### ج. موديلات عامة بدل مكررة (Design for Reuse)
لو حاجة هتتكرر في أكتر من كيان (حتى لو دلوقتي محتاجينها في كيان واحد بس)، تتبنى كموديل عام من الأول:
- **Tag واحد عام** يخدم الطلبات دلوقتي (مرحلة 4) والمنتجات/العملاء بعدين — مش موديل لكل كيان.
- **Audit Log / Chatter عام** (مرحلة 2) بدل `OrderLog` المقفول على الطلبات.
- **Product/Record Picker component واحد** (بحث + chips بـ htmx) يتعاد استخدامه في: منتجات مشابهة/مكمّلة (مرحلة 5)، Tags (مرحلة 4)، أي علاقة اختيار مستقبلية — مش نبنيه من الصفر كل مرة.

### د. توسّع من غير كسر
- أي حقل جديد على موديل موجود يتضاف كـ **nullable/blank بقيمة افتراضية آمنة**، وسلوك النظام القديم يفضل شغال زي ما هو لو الحقل الجديد فاضي (مثال مطبّق فعلًا في تصميم مرحلة 6: `ClientProfile.min_order_amount` لو فاضي بيرجع للقيمة العامة القديمة تلقائيًا).
- ممنوع تعديل سلوك موجود يعتمد عليه جزء تاني من غير ما تتوثّق نقطة التوافق (backward compatibility) في نفس الـ commit/PR.

### هـ. ثبات الشكل والأداء (زي ما كان متفق عليه)
- نفس الـ spacing، نفس الأزرار، نفس سلوك الجداول في كل صفحة.
- lazy loading + server-side pagination إلزامي على أي جدول جديد، مفيش "حمّل كل حاجة".
- **أي علاقة جديدة على صفحات المتجر (Product Variants، مشابه/مكمّل) لازم تُجلب بـ `select_related`/`prefetch_related` — استعلام واحد للصفحة، مش query جوه loop.** على شبكة المنتجات (الصفحة الرئيسية/الأقسام) بالذات، ممنوع أي حساب لكل كارت لوحده (N+1) — لو محتاج بيانات محسوبة لكل كارت، تُجلب بـ `annotate()` على الـ queryset الأساسي مرة واحدة. (هي دي بالظبط السبب اللي خلّانا نلغي فكرة "شرط إظهار تلميح الـ hover" ونستبدلها بتلميح ثابت بلا استعلام — راجع مرحلة 5).
- الفلسفة: بنقلّد بنية Odoo (علاقات عميقة، تنقل واعي بالسياق) مش شكله، مع الحفاظ على هوية BioZone البصرية.
- أي مرحلة فيها migration (2، 5، 6) لازم تتعمل في فرع منفصل وتتختبر بمعزل عن باقي المراحل قبل الدمج.

---

## 3. للمطوّر القادم — من فين تبدأ

لو هتكمل الشغل وملكش سياق المحادثة الأصلية: **اقرا قسم 2 (المبادئ المعمارية) الأول قبل أي سطر كود** — كل مرحلة لازم تتنفذ بالقواعد دي بالظبط، مش بس الوصف الوظيفي. بعد كده: ابدأ بمرحلة 1 (الأضمن والأقل خطورة)، وبالتوازي حد تاني يقدر يبدأ في تصميم موديل مرحلة 2 (Audit Log) لأنها مستقلة تمامًا. قضية bidi في `name_ar` **اتحسمت** (شوف قسم 0 ومرحلة 5) — الحل هو `Product.size` + `ProductVariantGroup`، مش patch مؤقت.
